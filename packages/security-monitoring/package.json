{
  "name": "@cloudshield/security-monitoring",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --env-file-if-exists=../../.env.test --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@cloudshield/contracts": "workspace:*",
    "@cloudshield/database": "workspace:*",
    "@cloudshield/utils": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
