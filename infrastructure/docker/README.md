# Docker Runtime

Docker Compose runs the local CloudShield foundation stack.

- Frontend: `http://localhost:3100`
- Backend: `http://localhost:4100`
- Postgres: `localhost:55432`
- Redis: `localhost:6381`

Container-to-container traffic uses standard service ports.
