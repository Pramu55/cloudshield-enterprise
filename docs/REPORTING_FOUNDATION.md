# Reporting Foundation

Report records are stored in `ReportExport` and remain database-backed.

Reports must:

- be organization-scoped
- record requestor/generator fields where available
- store status
- use safe structured output
- label sample data
- avoid official audit or certification claims

This milestone does not add fake PDF exports. JSON/HTML preview records remain the valid foundation.
