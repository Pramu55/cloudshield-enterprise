import { DashboardPage } from "../shared";

export default function AccountsPage() {
  return (
    <DashboardPage
      title="AWS Accounts"
      description="Registry for account ID, environment, owner team, regions, connection status, and posture scores."
    />
  );
}
