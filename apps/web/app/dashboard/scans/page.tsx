import { DashboardPage } from "../shared";

export default function ScansPage() {
  return (
    <DashboardPage
      title="Scan History"
      description="Foundation for read-only scan lifecycle, job phases, failure states, timestamps, and account coverage."
    />
  );
}
