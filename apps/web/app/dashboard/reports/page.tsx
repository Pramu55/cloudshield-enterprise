import { DashboardPage } from "../shared";

export default function ReportsPage() {
  return (
    <DashboardPage
      title="Reports"
      description="Export shell for executive summaries, security posture reports, cost waste reports, compliance evidence, and risk acceptance."
    />
  );
}
