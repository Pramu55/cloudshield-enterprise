import { DashboardPage } from "../shared";

export default function GraphPage() {
  return (
    <DashboardPage
      title="Cloud Risk Graph"
      description="Relationship-aware foundation for understanding paths between internet exposure, compute, IAM, storage, and encryption state."
    />
  );
}
